import Sidebar from './Sidebar';
import Header from './Header';

export default function Layout({ children, title }) {
return ( <div className="layout"> <Sidebar /> <main> <Header title={title} /> <div className="content">
{children} </div> </main> </div>
);
}
